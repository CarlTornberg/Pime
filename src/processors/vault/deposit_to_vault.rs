use pinocchio::{ProgramResult, account_info::AccountInfo};

pub fn process_deposit_to_vault(accounts: &[AccountInfo], instrution_data: &[u8]) -> ProgramResult {
    
    // Check if vault exists

    ProgramResult::Ok(())
}
